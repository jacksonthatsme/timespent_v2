= form_for @video, :html => { :multipart => true } do |f|
	= f.label :artist
	= f.text_field :artist

	= f.label :description
	= f.text_area :description

	= f.label :location
	= f.text_field :location

	= f.label :video
	= f.file_field :video
	
	= f.label :photo
	= f.file_field :photo

	= f.submit